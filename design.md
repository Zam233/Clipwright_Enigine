# 帧艺（ClipWright）内容视频编排引擎设计方案

---

## 1 方案概述

### 1.1 项目定位

帧艺（ClipWright）是一个**前后端分离的 AI 辅助视频创作系统**。它不是"一键生成视频"的工具，而是一个**人在回路的智能视频编辑器**——AI Agent 负责结构化和体力活，人负责审美判断和微调，两者在统一的编辑环境中循环迭代。

核心定位：**AI 副驾驶，而非自动驾驶。**

### 1.2 核心洞察

创作者的隐性知识分布在三个层面：

| 层面 | 内容 | 参数化方式 |
|------|------|-----------|
| **语言层** | 措辞密度、句式节奏、论证推进方式 | Persona 语言参数 |
| **视听层** | 剪切手感、动画偏好、配色直觉、BGM 情绪匹配 | Persona 视觉/音频参数 |
| **结构层** | 如何从选题切入、如何抛出论证、如何在关键处停顿 | Persona 节奏参数 + 示例层 |

帧匠将这三层全部参数化为 **Persona 配置**。Agent 按 Persona 出初稿，人在编辑器里审视、调整、确认——这个循环重复得越多，Persona 越贴合创作者。

### 1.3 产品形态

- **后端**：Persona 驱动的 Agent 引擎（Python + FastAPI + LangGraph），对外暴露 REST API，不依赖任何前端
- **前端**：完整的 Web 视频编辑器（React 19 + TypeScript），多轨时间轴对标 PR 基础能力
- **CLI 工具**：面向开发者和高级用户的命令行工具，`upforge` 系列命令

### 1.4 目标用户

| 用户类型 | 技术门槛 | 使用方式 | 投入程度 |
|----------|---------|---------|----------|
| 新 UP 主 | 低 | 选模板 + 填写选题 | 15 分钟 |
| 独立创作者 | 中 | 配置 Persona + 手动微调 | 持续投入 |
| 头部 UP 主 / MCN | 高 | LoRA 微调 + 示例层标注 | 深度定制 |

### 1.5 特化三级跳

```plaintext
Level 0 ─── 零配置 ──────── 选 Plugin 用默认参数，Agent 出初稿后手动调整
         │                   适用场景：临时项目、快速试剪
         │
Level 1 ─── Persona 配置 ─── 填写 YAML，Agent 按你的风格出初稿
         │                   适用场景：常规创作
         │
Level 2 ─── LoRA 微调 ───── 上传 50+ 历史视频，微调 Agent 底层模型
                             适用场景：头部 UP 主、MCN
```

---

## 2 技术特点

### 2.1 总体架构：五层分离

帧艺采用严格的分层架构，自底向上共五层：

```plaintext
┌─────────────────────────────────────────────────┐
│              用户接口层 (CLI / Web / API)           │
├─────────────────────────────────────────────────┤
│              Persona 配置层 (YAML/JSON)             │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│   │扎姆·知识区│  │何同学·数码│  │影视飓风· │  ...  │
│   │批判型    │  │创意型    │  │工业型    │       │
│   └──────────┘  └──────────┘  └──────────┘       │
├─────────────────────────────────────────────────┤
│              类型插件层 (Video Type Plugins)       │
│   ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐           │
│   │知识│ │鬼畜│ │数码│ │Vlog│ │纪录│  ...        │
│   │长片│ │快剪│ │评测│ │日常│ │短片│           │
│   └────┘ └────┘ └────┘ └────┘ └────┘           │
├─────────────────────────────────────────────────┤
│              Agent 编排层 (LangGraph Orchestrator)  │
│   ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐          │
│   │结构  │ │素材  │ │剪辑  │ │质检  │  ...       │
│   │Agent │ │Agent │ │Agent │ │Agent │          │
│   └──────┘ └──────┘ └──────┘ └──────┘          │
├─────────────────────────────────────────────────┤
│              原子能力层 (Atomic Capabilities)      │
│   FFmpeg │ OpenCV │ Manim │ Whisper │ TTS │ ... │
└─────────────────────────────────────────────────┘
```

**关键设计约束**：Persona 配置层不直接调用原子能力，必须经过类型插件层的翻译。即 Persona 中的 `cut_density: high` 由知识区长片插件翻译为具体的 `min_cut_interval_ms: 800` 等数值参数，而非被直接执行。

### 2.2 核心创新：Persona 四层复合体系

Persona 是帧艺最核心的技术创新。它不是一个简单的"风格预设"，而是一个**可训练、可迁移的创作者数字人格包**，由四种类型层叠加构成：

```plaintext
┌─────────────────────────────────────────┐
│              Persona 复合体               │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │  类型一：参数层 (Parameter)      │    │
│  │  YAML / JSON                    │    │
│  │  规则的、可读的、人可编辑的       │    │
│  └─────────────────────────────────┘    │
│              ↓ 叠加                     │
│  ┌─────────────────────────────────┐    │
│  │  类型二：示例层 (Exemplar)       │    │
│  │  视频片段 + 标注                │    │
│  │  "这种地方我会这样剪"            │    │
│  └─────────────────────────────────┘    │
│              ↓ 叠加                     │
│  ┌─────────────────────────────────┐    │
│  │  类型三：嵌入层 (Embedding)      │    │
│  │  向量 + 特征分布                │    │
│  │  不可读、可计算、可比较           │    │
│  └─────────────────────────────────┘    │
│              ↓ 叠加                     │
│  ┌─────────────────────────────────┐    │
│  │  类型四：模型层 (Model)          │    │
│  │  LoRA / Adapter 权重            │    │
│  │  深度特化、可直接推理             │    │
│  └─────────────────────────────────┘    │
│                                         │
│  层与层之间：继承关系、覆盖规则、冲突消解 │
└─────────────────────────────────────────┘
```

#### 2.2.1 四层详解

**参数层（Parameter Layer）**：显式的、人可读的风格约束。包含身份、语言、节奏、视觉、音频、约束六个维度。所有字段可用 `git diff` 比较差异，支持版本控制。

**示例层（Exemplar Layer）**：由带标注的视频片段组成，解决"规则说不清"的创作判断问题。Agent 在决策时检索同类标注的 exemplar，提取其剪辑参数分布作为 few-shot 参考。

**嵌入层（Embedding Layer）**：用模型自动提取创作者的隐性特征向量，包括节奏嵌入（128 维）、视觉嵌入（512 维）、语言嵌入（768 维）、论证结构嵌入（384 维）。Agent 计算当前决策与历史分布的距离，偏差过大时触发警告。

**模型层（Model Layer）**：包含微调过的模型权重（LoRA / Adapter），可替代通用模型参与推理。包括结构 Agent LoRA、剪辑 Adapter、动画 LoRA、声音克隆等。

#### 2.2.2 四层叠加执行逻辑

```plaintext
Agent 决策点
    │
    ├──→ 1. 加载模型层权重（如果有）→ 替代通用模型做基础推理
    ├──→ 2. 查询嵌入层 → 计算决策与历史分布距离（>2σ 标记偏离）
    ├──→ 3. 检索示例层 → 匹配同类标注的 exemplar 作为 few-shot 参考
    └──→ 4. 执行参数层硬约束 → 检查 forbidden_patterns、max_duration 等
```

#### 2.2.3 Persona 继承与组合

Persona 支持继承、覆盖、组合，形成谱系树：

- **继承**：子 Persona 继承父 Persona 的全部四层
- **覆盖**：修改特定层的特定字段
- **组合**：引入另一个 Persona 的某层，以权重混合

### 2.3 类型插件系统

不同的视频类型其底层剪辑逻辑差异巨大，帧艺通过类型插件层来封装这些差异：

| 维度 | 知识区长片 | 鬼畜区快剪 | Vlog 日常 |
|------|-----------|-----------|----------|
| 平均镜头时长 | 5-15s | 0.3-2s | 3-8s |
| 转场类型 | 硬切为主 | 闪白 / Jump Cut | 缓入缓出 |
| BGM 角色 | 环境铺垫 | 节奏骨架 | 情绪引导 |
| 动画密度 | 中（关键词标注） | 极高（全屏特效） | 低（字幕为主） |
| 停顿设计 | 有（留白思考） | 无（不能停） | 有（情绪呼吸） |

**关键设计**：同一个 Persona 配置套用不同的类型插件，会产生不同但风格一致的视频。扎姆做知识区长片和鬼畜二创，剪辑参数不同，但"扎姆感"由 Persona 保证。

预置插件：
- `knowledge_longform` — 知识区长片
- `kichiku_fastcut` — 鬼畜快剪
- `digital_review` — 数码评测
- `vlog_daily` — Vlog 日常

### 2.4 前端编辑器技术栈

| 层 | 选型 | 理由 |
|---|------|------|
| 框架 | React 19 + TypeScript | 生态成熟，状态管理灵活 |
| 状态管理 | Zustand | 轻量，适合编辑器复杂状态 |
| 时间轴 | 自研 Canvas + DnD Kit | 需要帧级精度，现有开源方案不够 |
| 视频预览 | WebCodecs API + Canvas | 无需后端转码即可实时预览 |
| UI 组件 | Radix UI + Tailwind | 无样式锁定的基础组件 |
| 与后端通信 | TanStack Query + WebSocket | REST 数据同步 + 实时状态推送 |

### 2.5 后端技术栈

| 层 | 选型 | 理由 |
|---|------|------|
| Agent 编排 | LangGraph | DAG 表达力强，支持条件分支和人机协作 |
| API 框架 | FastAPI | 异步原生，Python 生态最成熟的 Web 框架 |
| 视频处理 | FFmpeg + MoviePy | 工业标准，API 稳定 |
| 视觉理解 | CLIP + SigLIP | 开源，能做语义级素材匹配 |
| 动画生成 | Manim Community | 程序化动画，比 AE 脚本更可控 |
| 语音合成 | GPT-SoVITS | 开源，Few-shot 声音克隆 |
| 模型推理 | vLLM / llama.cpp | 本地部署，数据不出创作者机器 |
| Persona 存储 | YAML + SQLite | YAML 人可读，SQLite 做版本管理 |

### 2.6 关键设计原则

1. **通用层只做"视频创作"的抽象，不做"视频风格"的假设。** 一个转场 API 不应该内嵌特定创作者的预设。
2. **Persona 是驱动核心，不是外层皮肤。** 换 UP 主不是换个配色方案，是从剪辑节奏到论证逻辑的全面接管。
3. **插件即类型。** 不同视频类型的差异大到不能用参数覆盖，必须用独立插件。
4. **前后端统一时间线 JSON 格式。** Agent 输出和用户手工编辑在同一个数据模型上工作，确保互操作性。
5. **原子能力 API 只接受纯数值或路径参数。** `cut_density: high` 是上层逻辑，原子层只认 `min_cut_interval_ms: 800`。

### 2.7 第三方插件扩展体系

系统支持四种扩展方式：

| 扩展类型 | 运行位置 | 示例 |
|----------|---------|------|
| 素材库插件 | 后端 | 增加新的视频/图片/音效素材源 |
| 编辑器插件 | 前端（浏览器） | 新增 UI 组件、修改面板、添加交互工具 |
| Agent 插件 | 后端 | 新增 Agent 节点、替换策略、添加质检规则 |
| 能力工具插件 | 后端 | 封装对特定工具/服务/模型的调用 |

---

## 3 业务工作流

### 3.1 完整视频生产流程

```plaintext
[选择/创建 Persona] → [选择视频类型插件] → [输入选题] → [Pipeline 自动执行]
    ↑                                                                  │
    └──────────────────────── 循环迭代 ────────────────────────────────┘
                                                                        ↓
                                                                  [人工微调]
                                                                        ↓
                                                                  [渲染导出]
```

### 3.2 七 Agent 编排管线（LangGraph DAG）

帧艺的核心工作流由 7 个 Agent 通过 LangGraph 编排为有向无环图（DAG）：

```plaintext
                    ┌──────────┐
                    │ 触发信号  │
                    └────┬─────┘
                         ↓
              ┌──────────────────┐
              │ ① 结构 Agent      │
              │ 输入：选题/热点    │
              │ 输出：脚本骨架     │
              └──────┬───────────┘
                     ↓
         ┌───────────────────────┐
         │ ② 素材 Agent          │
         │ 输入：脚本骨架+素材库  │
         │ 输出：候选素材集合     │
         └──────┬────────────────┘
                ↓
    ┌───────────────────────────┐
    │ ③ 剪辑 Agent              │ ← Persona 参数在此注入
    │ 输入：素材+时间轴模板     │
    │ 输出：粗剪时间线          │
    └──────┬────────────────────┘
           ↓
    ┌───────────────────────────┐
    │ ④ 动画 Agent              │
    │ 输入：粗剪+动画参数       │
    │ 输出：带动画的时间线      │
    └──────┬────────────────────┘
           ↓
    ┌───────────────────────────┐
    │ ⑤ 音效 Agent              │
    │ 输入：时间线+音频参数     │
    │ 输出：混音后的时间线      │
    └──────┬────────────────────┘
           ↓
    ┌───────────────────────────┐
    │ ⑥ 质检 Agent              │
    │ 输入：完整时间线+规则集   │
    │ 输出：pass / fail + 修正  │
    └──────┬────────────────────┘
           ↓
       ┌───────┐
       │ 渲染  │
       └───────┘
```

每个 Agent 内部有一个**策略注册表**，根据 Persona 配置和视频类型插件动态选择策略。Agent 不写死逻辑，它只是"调度器"。

### 3.3 逐 Agent 详细拆解

#### 3.3.1 结构 Agent（StructureAgent）

- **输入**：选题描述 / 热点话题 / 脚本大纲
- **输出**：带时间戳标记的分镜脚本（Storyboard）
- **Persona 注入**：读取 `identity.tone`、`knowledge_domains`、`language` 参数
- **核心能力**：结构化的"信息起伏"规划。输出标记如 `[HOOK]`、`[BUILD]`、`[IMPACT]`、`[RESOLVE]`，控制语速、画面密度、留白时长的节奏变化

#### 3.3.2 素材 Agent（MaterialAgent）

- **输入**：分镜脚本 + 素材库索引
- **输出**：候选素材集合（按场景分组，带匹配分数）
- **核心技术**：CLIP 语义检索 + 概念映射（如"认知过载"→ 多窗口终端界面、数据流瀑布，而非"头痛的人"）
- **检索来源**：本地素材库、AI 匹配推荐、历史使用素材

#### 3.3.3 剪辑 Agent（EditAgent）

- **输入**：候选素材 + 时间轴模板
- **输出**：粗剪时间线 JSON
- **Persona 注入点**：这是 Persona 参数决定性的注入节点
  - 类型插件将 Persona 参数翻译为剪辑参数
  - 跳切控制：知识类 4-8 秒/镜头，逻辑转折点强制 0.3 秒硬切
  - 信息分层：口播密度高时自动触发"主画面+信息卡片"布局
  - 防疲劳：连续同构图超过 3 个时自动插入缩放关键帧
- **支持模式**：全流程执行/单 Agent 执行/局部重执行

#### 3.3.4 动画 Agent（AnimationAgent）

- **输入**：粗剪时间线 + 动画参数
- **输出**：带动画效果的时间线
- **关键处理**：
  - 文字动效参数化（Typewriter、Ease-Out Quad、Glitch 等）
  - 转场逻辑（硬切为主，关键节点用 Pixel-Dissolve 或 Glitch）
  - 关键术语自动高亮（颜色、背景网格线）
  - 版式遵循三分法网格

#### 3.3.5 音效 Agent（AudioAgent）

- **输入**：时间线 + 音频配置
- **输出**：混音后的时间线
- **核心逻辑**：为逻辑服务，不为煽情服务
  - 推导段：低频电子/氛围音（-24dB 以下），不抢人声
  - 结论段：BGM 瞬间抽离（-inf dB），制造听觉真空强化观点
  - 提示音：文字出现匹配机械键盘或 UI 交互音效

#### 3.3.6 质检 Agent（QualityAgent）

- **输入**：完整时间线 + 规则集
- **输出**：pass/fail + 修正指令
- **四项检查**：
  1. **节奏方差检测**：镜头时长标准差若低于阈值，触发"剪辑太匀速"警告
  2. **视觉规范核对**：检测字体、配色、安全区是否偏离 Persona 配置
  3. **音量峰值检查**：全片 LUFS 在 -14 至 -16 之间，无突兀爆音
  4. **逻辑连贯性检查**：转场前后画面语义是否断裂

### 3.4 支持的工作流模式

| 模式 | API |
|------|-----|
| 全流程执行 | `POST /api/pipeline/run` |
| 单 Agent 执行 | `POST /api/pipeline/step/{agent}` |
| 局部重执行 | `POST /api/pipeline/retry/{id}` |
| Agent 建议 | `POST /api/agent/suggest/*` |

### 3.5 人在回路的工作流

帧艺不是"一键出片"，而是创造了一个**循环迭代**的创作环境：

```plaintext
Agent 生成初稿
    ↓
前端编辑器加载时间线 JSON
    ↓
创作者在时间轴上逐帧审视、手动微调
    ↓
局部不满意 → 选中区域 → 让 Agent 重新处理该段 → 回到上一步
    ↓
全部满意 → 输出渲染
```

编辑器中的"Agent 副驾驶面板"支持三种介入方式：
1. **全局 Agent 调用**：输入选题，生成完整初稿
2. **局部 Agent 调用**：选中时间轴空白区域或素材，让 Agent 处理局部
3. **建议模式**：Agent 持续分析当前时间线，列出优化建议

### 3.6 完整新用户接入流程

```bash
# Step 1: 初始化 Persona
upforge persona init --name "老陈·数码毒舌" --tone tech_enthusiast

# Step 2: 上传采样视频（自动分析生成 Persona）
upforge persona analyze --video-dir ./samples/

# Step 3: 人工微调 YAML
vim personas/laochen_tech_sarcastic.yaml

# Step 4: 试运行（仅生成时间线预览）
upforge run persona=laochen_tech_sarcastic \
  plugin=digital_review \
  topic="某品牌新手机发热问题深度分析" \
  --dry-run

# Step 5: 满意后正式渲染
upforge run ... --render --output ./ep01.mp4

# Step 6（可选）：LoRA 深度特化
upforge finetune persona=laochen_tech_sarcastic \
  agent=edit_agent \
  --videos ./all_videos/
```

### 3.7 前后端通信 API 设计

```
# Pipeline 执行
POST   /api/pipeline/run              # 全流程执行
POST   /api/pipeline/step/{agent}     # 单 Agent 执行
POST   /api/pipeline/retry/{id}       # 局部重执行

# Agent 建议
POST   /api/agent/suggest/clip        # 推荐素材
POST   /api/agent/suggest/pace        # 节奏分析
POST   /api/agent/suggest/style       # 风格一致性

# Persona 管理
GET    /api/persona/list
POST   /api/persona/create
POST   /api/persona/analyze           # 从视频自动生成

# 素材库
GET    /api/library/search            # 语义检索
POST   /api/library/upload

# 渲染
POST   /api/render/start
GET    /api/render/status/{id}

# 实时通信
WS     /ws/agent/stream               # Agent 执行状态推送
```

---

## 4 路线图

| 阶段 | 内容 | 状态 |
|------|------|------|
| **Phase 1** | 后端：单 Agent 链条 + API；前端：基础时间轴编辑器 | **开发中** |
| **Phase 2** | 多类型插件；前端动画面板 + Agent 副驾驶面板 | 规划中 |
| **Phase 3** | LoRA 微调管线；素材库语义检索；渲染队列 | 规划中 |
| **Phase 4** | 插件市场 + Persona 市场 + 协作编辑 | 远期 |
